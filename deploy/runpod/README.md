# RunPod

RunPod setup, pinned environment details, and exact launch commands belong here once validated on a pod.

Record the GPU model, driver, CUDA version, vLLM version, model revision, storage configuration, and exposed ports for every reproducible run.
