# Observability

Prometheus configuration, Grafana dashboards, and definitions for every published metric.

At minimum, track Time to First Token (TTFT), Time Per Output Token (TPOT), throughput, running requests, waiting requests, and Key-Value (KV) cache utilization.
