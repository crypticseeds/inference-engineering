# Service

Shared code and configuration for the OpenAI-compatible inference service.

Keep engine-specific launch configuration under `variants/` or `deploy/`. Add application code here only when the roadmap introduces behavior outside the serving engine, such as request middleware.
