# Variants

Alternative engines and optimization configurations measured against the baseline.

Change one variable at a time and keep the hardware, model family, prompts, token lengths, and benchmark method fixed.
