# AWQ

Activation-aware Weight Quantization (AWQ) configuration and benchmark results against the baseline.

Record memory, throughput, latency, and the same quality proxy used for other precision variants.
